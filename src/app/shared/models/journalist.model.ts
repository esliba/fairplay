export class Journalist {
    constructor(public id: number, public name: string, 
                public phrase: string, public full_information: string,
                public image_path: string
                ) {}
}